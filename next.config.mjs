/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "github.com",
      "images.pexels.com",
      "media.istockphoto.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
